[package]
name = "eismaster-cli"
version = "0.1.0"
edition = "2021"
description = "CLI for EISMaster core operations"

[dependencies]
eismaster-core = { path = "../eismaster-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
